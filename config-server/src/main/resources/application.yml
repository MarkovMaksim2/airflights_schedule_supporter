server:
  port: ${PORT:9991}

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: http://github. com/MarkovMaksim2/config-server.git
          default-label: main
          search-paths: "{application}"